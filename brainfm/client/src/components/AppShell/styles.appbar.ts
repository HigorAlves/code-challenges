import { createStyles } from '@mantine/core'

export default createStyles(() => ({
	container: {
		paddingTop: 5,
		display: 'flex',
		alignItems: 'center',
		justifyContent: 'space-between',
		flexWrap: 'wrap'
	}
}))
